sync:
  defaults:
    mode: "one-way-replica"
    ignore:
      vcs: true
      paths:
        - "data/"
        - "experiments/"
        - "wandb/"
        - "logs/"
        - ".venv/"
        - ".env"
        - "__pycache__/"
        - "*.egg-info/"
        - "**/*.ckpt"
        - "**/*.pt"
        - "**/*.pyc"
  transformer-jz:
    alpha: "."
    beta: "jz:/lustre/fswork/projects/rech/imi/uix29qp/proj/transformer"   # ← fix this path
  transformer-adastra:
    alpha: "."
    beta: "adastra:/lus/work/CT10/iso1996/mcvjetko/proj/transformer"
    